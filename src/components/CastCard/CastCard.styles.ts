import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    alignItems: 'center',
    marginRight: 15,
  },
  profileImage: {
    width: 80,
    height: 80,
    borderRadius: 40,
    marginBottom: 5,
  },
  name: {
    fontSize: 14,
    color: '#fff',
    textAlign: 'center',
  },
});

export default styles;
